{{- $entries := concat (.Values.commonServiceEntries | default list) (.Values.serviceEntries | default list) }}
{{- range $entries }}

---
{{- include "istio-service-entry.render" (dict "root" $ "entry" .) }}
{{- end }}
